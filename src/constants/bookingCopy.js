/**
 * Customer-facing copy when slots are blocked by a special event or the API
 * returns special_event_message. Intentionally omits internal event titles.
 */
export const SPECIAL_EVENT_AVAILABILITY_MESSAGE =
    'No availability, please select another date.';
